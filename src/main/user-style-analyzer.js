class UserStyleAnalyzer {
	/** @param {Array<{role: string, content: string}>} _messages */
	async analyze(_messages) {
		// prototype stub — returns empty profile
		return {};
	}
}

module.exports = { UserStyleAnalyzer };
