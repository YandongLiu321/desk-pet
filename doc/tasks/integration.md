# 集成联调与收尾

> 对应提案第四阶段 (Day 11-14)：三模式切换流程完善、数据同步验证、异常处理、类型检查、基础单元测试。

## 子任务

### 三模式切换流程

- [ ] **int-01** 验证桌宠→壁纸→软件三模式任意切换不闪退
- [ ] **int-02** 验证模式切换时：当前窗口隐藏 → 目标窗口显示 → mode:activated 通知到达
- [ ] **int-03** 验证桌宠模式数据更新后，切换到软件模式能读取最新数据
- [ ] **int-04** 验证壁纸模式番茄钟运行时切换到桌宠，番茄钟自动 stop

### 数据同步

- [ ] **int-05** 验证：桌宠模式创建任务 → 软件模式可看到
- [ ] **int-06** 验证：软件模式子任务勾选 → 桌宠迷你面板状态同步
- [ ] **int-07** 验证：对话在桌宠和软件模式间共享（同一 conversationStore 数据源）
- [ ] **int-08** 验证：lowdb 数据文件正常写入，重启应用后数据保留

### 异常处理

- [ ] **int-09** LLM API 不可用时 → 角色化错误提示（"星辰通讯中断…"）
- [ ] **int-10** API Key 未配置时 → 友好提示配置方法
- [ ] **int-11** lowdb 文件损坏时 → 自动重建默认数据不崩溃
- [ ] **int-12** 窗口创建失败时 → 记录错误日志 + 不影响其他窗口
- [ ] **int-13** 流式对话中途切换模式 → 流正常中止，已接收内容不保存

### 代码质量

- [ ] **int-14** 运行 `npx tsc --noEmit` 类型检查通过
- [ ] **int-15** 运行 `npx biome check` lint 通过
- [ ] **int-16** 运行 `npx vitest run` 所有单元测试通过

### 手动验收

- [ ] **int-17** 右键菜单发布任务流程跑通（右键→发布任务→输入待办 → AI 转化 → 任务出现在面板）
- [ ] **int-18** 番茄钟流程跑通（启动 → 倒计时 → 到时间 → 完成确认 → ESC退出 → 任务进度询问 → 完成则勾选子任务/未完成则询问延长时间（同意重启番茄钟/拒绝记录进度） → 返回桌宠）
- [ ] **int-19** 任务结算流程跑通（勾选子任务 → 提交 → 叙事反馈显示 + 章节里程碑推进验证）
- [ ] **int-19b** 对话智能模式切换跑通（"想开始专注了" → AI 询问确认 → 用户同意 → 自动切换到壁纸模式）

### 文档

- [ ] **int-20** 补充 README.md 开发运行说明（npm install → npm run dev → npm start）
- [ ] **int-21** 扫描 `src/main/ipc-handlers.js` 和 `src/preload.js`，验证无裸写 IPC channel 字符串（所有 channel 名必须从 `src/shared/constants.js` 引用）
